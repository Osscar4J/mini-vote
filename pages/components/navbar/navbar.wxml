<view class="bg-gradual-blue padding text-center shadow-blur navbar text-cut">
    <view class="fl" wx:if="{{ back }}" catchtap="goback">返回</view>
    <view class="text-lg title">{{title}}</view>
</view>
<view class="mg"></view>